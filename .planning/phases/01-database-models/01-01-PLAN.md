---
phase: '01'
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/pipeline_stage.rb
  - app/models/contact.rb
  - app/models/account.rb
  - app/controllers/api/v1/accounts/contacts_controller.rb
autonomous: true
requirements:
  - CRM-01
  - CRM-05

must_haves:
  truths:
    - "Contact can optionally belong to a pipeline stage"
    - "New accounts automatically get a default pipeline with one 'New' stage"
    - "Contact API accepts pipeline_stage_id in update operations"
    - "PipelineStage records are ordered by position using acts_as_list"
  artifacts:
    - path: app/models/pipeline_stage.rb
      provides: PipelineStage model with belongs_to :account and acts_as_list
      min_lines: 20
      exports:
        - PipelineStage
        - belongs_to :account
        - acts_as_list
    - path: app/models/contact.rb
      provides: Contact belongs_to association for pipeline_stage
      contains: belongs_to :pipeline_stage, optional: true
    - path: app/models/account.rb
      provides: Account after_create callback for default pipeline stage
      contains: after_create_commit
    - path: app/controllers/api/v1/accounts/contacts_controller.rb
      provides: pipeline_stage_id in permitted params
      contains: pipeline_stage_id
  key_links:
    - from: app/models/contact.rb
      to: app/models/pipeline_stage.rb
      via: belongs_to :pipeline_stage
      pattern: belongs_to :pipeline_stage
    - from: app/models/account.rb
      to: app/models/pipeline_stage.rb
      via: after_create callback
      pattern: after_create_commit.*pipeline_stage|PipelineStage
---

<objective>
Create the CRM pipeline foundation: PipelineStage model, Contact pipeline_stage association, Account auto-creation callback, and Contact controller permitted params.

Purpose: Provides the data model layer for the CRM pipeline system. Contacts gain an optional pipeline_stage_id FK, and new accounts automatically get a default "New" stage.
Output: PipelineStage model, Contact association, Account callback, permitted params update
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@app/models/contact.rb
@app/models/account.rb
@config/routes.rb
@app/controllers/api/v1/accounts/contacts_controller.rb

<!-- Interface contracts the executor needs -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineStage model</name>
  <files>app/models/pipeline_stage.rb</files>
  <read_first>
app/models/contact.rb
app/models/account.rb
app/models/label.rb
  </read_first>
  <action>
Create `app/models/pipeline_stage.rb` following Chatwoot conventions:

```ruby
class PipelineStage < ApplicationRecord
  belongs_to :account
  acts_as_list scope: :account
  validates :name, presence: true
  validates :color, format: { with: /\A#[0-9A-Fa-f]{6}\z/, message: I18n.t('errors.pipeline_stage.color.invalid') }, allow_blank: true

  scope :sorted, -> { order(:position) }

  def color_with_hash
    color.present? ? color : '#6B7280'
  end
end
```

Key decisions (per D-01, D-02, D-03):
- No Pipeline model -- PipelineStage has account_id directly (D-01)
- acts_as_list for position management (D-02)
- color stored as hex string format '#FF5733' (D-03)
- Default color fallback '#6B7280' (gray) when no color set

Follow the same pattern as Label model (account-scoped, acts_as_list, validation).
Use `acts_as_list scope: :account` so stages are ordered per account.
Add `sorted` scope for default ordering.
  </action>
  <verify>
<automated>
ruby -c app/models/pipeline_stage.rb && grep -q "acts_as_list" app/models/pipeline_stage.rb && grep -q "belongs_to :account" app/models/pipeline_stage.rb
</automated>
  </verify>
  <done>
PipelineStage model exists at app/models/pipeline_stage.rb with acts_as_list, belongs_to :account, validates :name presence, and validates :color hex format
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pipeline_stage association to Contact model</name>
  <files>app/models/contact.rb</files>
  <read_first>
app/models/contact.rb
  </read_first>
  <action>
Add `belongs_to :pipeline_stage, optional: true` association to Contact model.

Read the existing Contact model to find the right insertion point. The association should be placed with the other belongs_to declarations (around line 58).

Add after the `belongs_to :account` line:
```ruby
belongs_to :pipeline_stage, optional: true
```

This is per D-06: Contact belongs_to :pipeline_stage, optional: true
The optional: true is critical -- existing contacts without a pipeline stage must continue to work.
  </action>
  <verify>
<automated>
grep -q "belongs_to :pipeline_stage, optional: true" app/models/contact.rb
</automated>
  </verify>
  <done>
Contact model has belongs_to :pipeline_stage, optional: true association
  </done>
</task>

<task type="auto">
  <name>Task 3: Add after_create callback on Account for default stage</name>
  <files>app/models/account.rb</files>
  <read_first>
app/models/account.rb
  </read_first>
  <action>
Add `after_create_commit :create_default_pipeline_stage` callback to Account model.

Read the existing Account model. Find the existing `after_create_commit :notify_creation` line (around line 150). Add a new private method `create_default_pipeline_stage` that creates a PipelineStage with name "New", position 1, and a default color.

Add the callback:
```ruby
after_create_commit :create_default_pipeline_stage
```

Add the private method (after `notify_creation`):
```ruby
def create_default_pipeline_stage
  pipeline_stages.create!(name: 'New', position: 1, color: '#22C55E')
end
```

Per ROADMAP.md: "one 'New' stage" auto-created on account creation. Use green (#22C55E) as default color following common "new/active" conventions.
  </action>
  <verify>
<automated>
grep -q "create_default_pipeline_stage" app/models/account.rb && grep -q "after_create_commit" app/models/account.rb
</automated>
  </verify>
  <done>
Account model has after_create_commit callback calling create_default_pipeline_stage method that creates a PipelineStage with name "New", position 1, and color '#22C55E'
  </done>
</task>

<task type="auto">
  <name>Task 4: Add pipeline_stage_id to Contact permitted params</name>
  <files>app/controllers/api/v1/accounts/contacts_controller.rb</files>
  <read_first>
app/controllers/api/v1/accounts/contacts_controller.rb
  </read_first>
  <action>
Add `pipeline_stage_id` to the Contact controller's permitted params.

Read the contacts_controller.rb. Find the `permitted_params` method (around line 173). Add `pipeline_stage_id` to the permit list:

```ruby
def permitted_params
  params.permit(:name, :identifier, :email, :phone_number, :avatar, :blocked, :avatar_url,
                :pipeline_stage_id, additional_attributes: {}, custom_attributes: {})
end
```

This allows pipeline_stage_id to be passed when creating or updating contacts via the API.
Per ROADMAP.md success criterion 3: "Contact update action accepts pipeline_stage_id in permitted params".
  </action>
  <verify>
<automated>
grep -q "pipeline_stage_id" app/controllers/api/v1/accounts/contacts_controller.rb
</automated>
  </verify>
  <done>
Contact controller permitted_params method includes pipeline_stage_id as a permitted parameter
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted pipeline_stage_id integer crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tamper | pipeline_stage_id param | mitigate | Validate pipeline_stage_id belongs to the same account when updating Contact; add strong params validation |
| T-01-02 | Denial | Account creation with callback | accept | after_create is fast; low risk of DoS |
| T-01-03 | Information | Contact pipeline_stage visibility | accept | Stage assignment is intended feature; no sensitive data exposed |

## Nyquist Compliance Note

VALIDATION.md was not generated because the research step was skipped (--auto mode with no existing research). Since this is a backend-only phase with no UI components, Nyquist validation (which focuses on visual/functional UI verification) is not applicable per the workflow's "Nyquist is not applicable" clause. The automated verification commands in each task's `<verify>` block provide continuous verification coverage.
</threat_model>

<verification>
- Ruby syntax check: `ruby -c app/models/pipeline_stage.rb app/models/contact.rb app/models/account.rb`
- Rails model check: `bin/rails runner "puts PipelineStage.column_names; puts Contact.column_names"` (will fail until migration runs but model files should syntax-check)
</verification>

<success_criteria>
- PipelineStage model created with belongs_to :account, acts_as_list, name/position/color fields
- Contact has belongs_to :pipeline_stage, optional: true
- Account has after_create_commit creating default "New" stage
- Contact controller permits pipeline_stage_id in params
- No migrations created (per D-04 -- deferred to later phase)
- All files follow Chatwoot Ruby conventions (rubocop checked)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-models/01-01-SUMMARY.md`
</output>
