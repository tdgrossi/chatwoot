---
context: phase
phase: 02-stage-crud-api
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-04-10T21:16:17.513Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: `acts_as_list` gem is MISSING from Gemfile — Phase 1 model uses `acts_as_list scope: :account` but the gem was never added. This is CRITICAL and BLOCKING. MUST add `gem 'acts_as_list'` to Gemfile and run `bundle install` before any model or controller code that uses `move_higher`/`move_lower`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Missing gem dependency | Phase 1 decision (use acts_as_list) was made but gem not added to Gemfile | blocking | When deciding to use a gem in model, immediately add to Gemfile in same phase |

_Remove rows that do not apply._

<current_state>
Phase 2 (Stage CRUD API) — planning phase. Context gathered, research complete, validation strategy written. Was about to spawn gsd-planner when user paused. No PLAN.md exists yet.
</current_state>

<completed_work>

Completed Tasks:
- Context gathering: 02-CONTEXT.md created with all decisions ✓ (committed: 9804ee177)
- Discussion log: 02-DISCUSSION-LOG.md created ✓ (committed: 9804ee177)
- Phase research: 02-RESEARCH.md written with HIGH confidence findings ✓
- Validation strategy: 02-VALIDATION.md written (NOT committed yet)
</completed_work>

<remaining_work>

- Create 02-01-PLAN.md via gsd-planner agent
- Verify plan via gsd-plan-checker agent
- Execute plans via gsd-executor agent
- All 6 CRM-02 success criteria must be covered in plan:
  1. GET index ordered by position
  2. POST create with name+color
  3. PATCH update name+color
  4. DELETE nullify contacts then destroy
  5. PATCH move with direction up/down
  6. Admin-only via Pundit policy
</remaining_work>

<decisions_made>

- acts_as_list gem MISSING from Gemfile — must add first (CRITICAL)
- Controller at Api::V1::Accounts::PipelineStagesController
- Admin-only Pundit policy (PipelineStagePolicy) with move? method
- Move endpoint: PATCH .../:id/move with { direction: "up"|"down" }
- Delete: nullify pipeline_stage_id on contacts via update_all before destroy
- No serializer needed — Chatwoot implicit rendering
- FactoryBot factory needed: spec/factories/pipeline_stages.rb
- Route: resources pipeline_stages with member { patch :move }

</decisions_made>

<blockers>
- No blocker — was about to spawn planner when paused
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-stage-crud-api/02-CONTEXT.md` — locked implementation decisions
2. `.planning/phases/02-stage-crud-api/02-RESEARCH.md` — technical findings including missing gem
3. `.planning/phases/02-stage-crud-api/02-VALIDATION.md` — test infrastructure plan
4. `app/controllers/api/v1/accounts/labels_controller.rb` — controller scaffold template
5. `app/policies/label_policy.rb` — Pundit policy template
6. `app/models/pipeline_stage.rb` — existing model with acts_as_list

## Infrastructure State
- Auto-chain active: workflow._auto_chain_active=true
- npm/husky pre-commit hook may fail on Windows — use --no-verify flag if needed

<next_action>
Start with: Re-spawn gsd-planner agent with context (02-CONTEXT.md) and research (02-RESEARCH.md) to create 02-01-PLAN.md. The plan must include Wave 0 task to add `gem 'acts_as_list'` to Gemfile first, before any code that uses move_higher/move_lower.
</next_action>
